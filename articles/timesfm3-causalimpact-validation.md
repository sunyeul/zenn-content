---
title: "TimesFM 3で介入効果を読む前に：A/A検証からBSTS比較まで"
emoji: "🔎"
type: "tech"
topics: ["timesfm", "因果推論", "時系列", "python", "colab"]
published: false
---

## はじめに

キャンペーン後に売上が増えた。その増加のうち、キャンペーンがなくても起きていた分はどのくらいでしょうか。

[CausalImpact](https://google.github.io/CausalImpact/CausalImpact.html)は、介入前の結果と、介入の影響を受けない対照時系列から「介入がなかった場合」を予測し、観測値との差を調べる方法です。そこで使う予測器に、事前学習済みの時系列モデルを使えたら便利そうです。

[TimesFM 3](https://research.google/blog/timesfm-3-a-zero-shot-foundation-model-for-multivariate-forecasting/)なら、追加学習なしに対照時系列を渡し、先の区間をまとめて予測できます。反実仮想の予測器を試すハードルが下がるのはうれしいところ。

ただ、**予測が外れた分も、観測値との差に入ります。** きれいな効果のグラフが描けても、その差を介入の効果として読めるかは、もう一段確かめたいですね。

この記事では、合成データを使って3つの段階を進めます。

- **Part A：予測器に共通する検証。** 過去の予測、時間A/A、既知の効果の注入を通じて、知りたい効果を誤差と区別できそうかを確認します。
- **Part B：TimesFMを使う実益。** 対照時系列の有無を比べ、追加学習なしの予測が今回の目的に役立つかを見ます。
- **Part C：固定した設定で効果を計算。** 前の検証で分かった精度と限界を踏まえ、介入後の差と累積効果を読みます。

まず分析方法としての妥当性を確かめ、次にTimesFMを選ぶ理由を考え、そのうえで効果の計算へ進む流れです。最後に、同じ条件のBSTSと比較し、予測器と区間の作り方による違いも確認します。

TimesFM 3の基本的な使い方や、関連データ・予定を加える予測は、前の記事[「TimesFM 3をColabで試す：関連データや予定を足すと予測は変わる？」](https://zenn.dev/sunyeul89/articles/timesfm-3-handson)で試しています。今回はそこから、予測を介入効果の分析に使う前の検証へ進みます。

## ノートブックを動かす準備

以下のボタンからColabを開き、GPUランタイムで上から実行してください。T4 GPU以上を推奨しています。

[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/sunyeul/zenn-content/blob/main/notebooks/timesfm3_causalimpact_bsts_validation_ja.ipynb)

以下のコードはノートブックからの抜粋です。データ生成、変数の準備、作図を含む実行順序はノートブックにまとめています。本文を実行した後、同じランタイムでBSTSの付録を実行できます。

```python
!pip install -q "timesfm[torch]==3.0.0" matplotlib pandas
```

今回はノートブックに合わせて `3.0.0` を使います。事前学習済み重みには [TimesFM Non-Commercial License v1.0](https://huggingface.co/google/timesfm-3.0-pytorch)が適用されるため、業務やサービスへの適用前に利用条件を確認してください。

Hugging Faceのモデルへのアクセスに認証が必要な場合は、モデルページで条件を確認し、Colabのシークレットに `HF_TOKEN` を保存します。ログイン処理はノートブックに含めています。

## まず、何を「効果」として計算するのか

### 観測値から「介入がなかった場合」を引く

介入がなかった場合の結果を $Y_t(0)$、その予測を $\hat Y_t(0)$ とすると、計算する差は次の形です。

$$
\hat\tau_t = Y_t^{\mathrm{obs}}-\hat Y_t(0)
$$

たとえば観測値が110、介入がなかった場合の予測が100なら、差は10です。ただし、その予測自体が10だけ低く外れていたなら、介入なしでも同じ差が出ます。

この差を因果効果として解釈するには、対照時系列が介入の影響を受けず、介入がない場合の関係を予測区間へ持ち越せることが必要です。[CausalImpactの公式説明](https://google.github.io/CausalImpact/CausalImpact.html)でも、この2点が前提になっています。同時期の別の変化や、介入前からの先行反応も確認したいところです。

TimesFMが直接返すのは、渡した情報に条件づけた予測です。観測値との差を取っただけで因果関係が確定するわけではありません。まずは **条件付き予測との差（Conditional Forecast Difference）** として読み、因果仮定と検証を併せて考えます。

### 今回は、真値が分かる合成データで試す

データは「ゆっくり変わる基礎水準 + 2本の対照時系列の線形な影響 + ノイズ」で作ります。

$$
\begin{aligned}
\mu_t &= \mu_{t-1}+\eta_t,\\
Y_t(0) &= \mu_t+1.4X_{1,t}-0.9X_{2,t}+\epsilon_t.
\end{aligned}
$$

対照時系列には周期的な動きがあり、介入の影響は加えません。結果変数については、同じ $Y(0)$ から「効果なし」と「介入後に10%上昇」の2つを作ります。

```python
effect_none = np.zeros(TOTAL, dtype=np.float32)

effect_positive = np.zeros(TOTAL, dtype=np.float32)
effect_positive[PRE:] = 0.10 * y0[PRE:]

y_null = y0 + effect_none
y_treated = y0 + effect_positive
```

`y_null` が効果なし、`y_treated` が効果ありの系列です。冒頭の図では観測値と真の $Y(0)$ を重ね、その下に対照時系列を表示します。ここで、**効果を加えたのは結果変数だけ**という対応を確認できます。

この構造はBSTSのlocal levelと回帰モデルで表現しやすい例です。CausalImpactの原論文のシミュレーションをそのまま再現したものでも、モデルの一般的な優劣を決めるデータでもありません。

#### データの表示

![観測値・真の反実仮想と、介入の影響を受けない対照時系列](/images/timesfm3-causalimpact/synthetic-data.png)
*観測値・真の反実仮想と、介入の影響を受けない対照時系列。*

### 本分析と同じ条件を、過去にも置く

今回の設定は、介入前512時点、効果を評価する期間64時点、予測に使う過去の長さ256時点です。介入前の最後の64時点は、最終A/Aのために残します。

```text
介入前：0〜511                                 介入後：512〜575
┌──────────────────────────┬───────────────┬───────────────┐
│ 開発用の予測・A/A・効果注入 │ 最後のA/A     │ 効果の計算例   │
│ 評価対象は447まで          │ 448〜511      │ 512〜575      │
└──────────────────────────┴───────────────┴───────────────┘
       各予測は直前256時点を入力し、先の64時点を評価する
```

64時点の効果を知りたいなら、検証も64時点先までそろえます。1時点先の予測がよいだけでは、期間全体の誤差は分かりません。

予測区間の実際のYは、ラグや移動平均、モデル更新にも使いません。一方、介入の影響を受けない対照時系列の同時点の値は、事後分析の条件として使います。将来の業績を事前に予測する場面とは、使える情報が違う点に注意してください。

## Part A：効果を読む前の共通検証

ここからの検証は、TimesFM以外の予測器でも必要です。まずモデルと入力を固定して、予測の誤差が効果の読み方にどう響くかを見ます。

### 1. 過去の複数区間で、予測の水準と動きを見る

TimesFMには、結果変数の過去と、予測区間まで含む対照時系列を渡します。ノートブックの予測関数は、介入後の実際のYを戻さず、一度に指定した長さを予測します。

```python
def run_counterfactual(model, y_context, horizon, covariates=None):
    """One-shot H-step forecast. Post-period target Y is never fed back."""
    y_context = np.asarray(y_context, dtype=np.float32)[None, :]

    kwargs = dict(
        context=y_context,
        horizon=horizon,
        return_quantiles=True,
        use_symmetric_averaging=False,
        make_positive=False,
    )

    if covariates is not None:
        kwargs["past_future_covariates"] = np.asarray(
            covariates, dtype=np.float32
        )

    out = model.predict(**kwargs)

    forecast = np.asarray(out.forecast)
    quantiles = np.asarray(out.quantiles)

    if forecast.ndim == 2:
        forecast = forecast[0]
    if quantiles.ndim == 3:
        quantiles = quantiles[0]

    return forecast, quantiles
```

`past_future_covariates` に渡すのは、介入の影響を受けない対照時系列です。たとえば入力が256時点、予測が64時点なら、対照時系列は両方を含む320時点分を用意します。

予測開始時点を16ずつずらしながら、同じ処理を繰り返します。

```python
CONTEXT = 256
DEV_H = H
FINAL_AA_CUT = PRE - H

# Overlapping windows are descriptive diagnostics, not independent trials.
DEV_CUTS = np.arange(
    CONTEXT,
    FINAL_AA_CUT - DEV_H + 1,
    16,
    dtype=int,
)
```

この設定では開始時点が256、272、…、384の9ウィンドウになります。どれも64時点を予測するので、評価区間は互いに重なります。

ノートブックでは各ウィンドウの観測値と予測値、MAE、平均符号付き誤差（ME）、16時点ずつに分けたMAEが出ます。

$$
e_{k,h}=Y_{k,h}-\hat Y_{k,h}(0),\qquad
\mathrm{MAE}_k=\frac{1}{H}\sum_h|e_{k,h}|,\qquad
\mathrm{ME}_k=\frac{1}{H}\sum_he_{k,h}.
$$

まずは予測線の形を見て、その後で数字を読むと分かりやすくなります。

| 出力がこう見えたら | 何を確認するか |
|---|---|
| 動きは似ているが、予測線がずっと下にある | 水準を低く予測しています。MEが正になり、その差が累積しないかを見ます。 |
| MEは0に近いのにMAEが大きい | 正負の誤差が相殺されているかもしれません。毎時点の予測が正確とは限りません。 |
| 一部のウィンドウだけ外れる | どの時期から線が離れるかを見ます。全体平均だけでは時期による違いが隠れます。 |
| 予測後半のMAEが大きい | 評価したい期間の最後まで、必要な精度を保てているかを確認します。 |

#### 予測検証の出力

![予測開始時点ごとのMAEと平均符号付き誤差](/images/timesfm3-causalimpact/backtest-errors.png)
*予測開始時点ごとのMAEと平均符号付き誤差。*

| 項目 | 値 |
| --- | --- |
| 検証ウィンドウ数（重複あり） | 9.000000 |
| ウィンドウ別MAEの中央値 | 1.094656 |
| ウィンドウ別MAEの最大値 | 1.961218 |

:::details 全9ウィンドウの予測経路・ホライズン別誤差・数値表

![9つの開発ウィンドウの観測値と予測経路](/images/timesfm3-causalimpact/backtest-paths.png)
*9つの開発ウィンドウの観測値と予測経路。*

![16時点ずつの予測区間で比較したMAE](/images/timesfm3-causalimpact/horizon-errors.png)
*16時点ずつの予測区間で比較したMAE。*

| 予測開始時点 | MAE | 観測平均 | MAE / 観測平均 (%) | ME（観測−予測） |
| --- | --- | --- | --- | --- |
| 256 | 1.305057 | 105.453987 | 1.237560 | 1.143004 |
| 272 | 1.066406 | 105.916595 | 1.006836 | 0.637930 |
| 288 | 0.994719 | 105.324829 | 0.944430 | 0.594236 |
| 304 | 1.250150 | 105.711487 | 1.182606 | 1.149795 |
| 320 | 0.843441 | 104.604752 | 0.806313 | -0.239029 |
| 336 | 1.094656 | 104.523117 | 1.047286 | -0.981061 |
| 352 | 1.062027 | 103.597923 | 1.025143 | -0.602010 |
| 368 | 1.802250 | 103.123962 | 1.747654 | -1.794038 |
| 384 | 1.961218 | 101.798943 | 1.926560 | -1.725563 |

:::

#### MAEが小さければ、それで十分？

説明用に、1日の平均結果が100、MAEが2のケースを考えます。誤差は普段の水準の2%ですが、知りたい変化が1日+1なら、その誤差は小さくありません。

さらに、毎日+2ずつ同じ方向に外れると、64日間の累積誤差は+128です。+2と−2が交互なら、MAEは同じでも累積誤差は0になり得ます。

**1時点の誤差の大きさと、期間全体の偏りは別々に見る必要があります。** 次は、その累積した差を直接確認します。

なお、表にある `MAE / 観測平均 (%)` は規模を読むための比率で、MAPEではありません。結果が0付近だったり正負を行き来したりするデータには、そのまま使わないでください。

### 2. 効果がなかった過去に、仮の介入を置く

過去の未介入区間を「ここで介入したことにする」と考え、観測値から予測値を引きます。これが時間プラセボ検証（in-time placebo）です。ノートブックでは時間A/Aと呼んでいます。無作為に分けた2群の比較とは異なります。

真の効果は0なので、64時点の差の合計は、予測誤差から生まれた**見かけの累積効果**です。

$$
E_k=\sum_{h=1}^{H}e_{k,h}=H\,\mathrm{ME}_k.
$$

この過去の累積誤差から、共通の偏りと、そこからどれだけ外れるかを求めます。

```python
def empirical_error_band(values, level=0.90):
    """
    Cumulative error distribution -> bias and symmetric error radius.
    Diagnostic only; not a formal conformal guarantee.
    """
    values = np.asarray(values, dtype=float)
    bias = np.median(values)
    radius = np.quantile(np.abs(values - bias), level)
    return bias, radius
```

`bias` は累積誤差の中央値、`radius` はその中央値からの絶対偏差の90%分位点です。見かけの効果から `bias` を引き、補正後の点の両側に `radius` を付けます。

各開発ウィンドウを評価するときは、評価対象のウィンドウを除いて補正値を計算します。

```python
def leave_one_out_band(values, i, level=0.90):
    others = np.delete(np.asarray(values, dtype=float), i)
    return empirical_error_band(others, level=level)
```

出力の `aa_result` は元の単位での点と区間、`aa_scale` は観測合計に対する割合です。図では0を含む区間が青、含まない区間が赤になります。

- **赤い区間が繰り返される**なら、介入なしでも効果があると読んでしまう可能性があります。
- **点が0付近でも区間が広い**なら、小さな効果との区別が難しいかもしれません。
- **開始時点ごとに正負が入れ替わる**なら、共通のバイアス補正がすべての時期に合うとは限りません。

ここでの90%は、誤差半径を作るときの分位点の設定です。**実際に90%の被覆率を持つ区間と保証しているわけではありません。** ウィンドウを1つ除いても残りと日付が重なるため、0を含まない割合も、独立した実験から推定した偽陽性率とは扱いません。

#### 時間A/Aの出力

![時間A/Aの補正後累積効果と経験的区間：元の単位と観測合計に対する割合](/images/timesfm3-causalimpact/placebo-intervals.png)
*時間A/Aの補正後累積効果と経験的区間：元の単位と観測合計に対する割合。*

| 項目 | 値 |
| --- | --- |
| 開発ウィンドウ数（重複あり） | 9.000000 |
| 共通の累積バイアス（全開発ウィンドウ） | -15.297844 |
| 共通の誤差半径（全開発ウィンドウ） | 96.014671 |
| 絶対偏差の分位点水準 | 0.900000 |

:::details ウィンドウ別の点・区間と規模の比較表

| 仮の介入時点 | 補正前の見かけの累積効果 | バイアス補正後の効果 | 区間下限 | 区間上限 | 0を含む |
| --- | --- | --- | --- | --- | --- |
| 256 | 73.152275 | 100.065525 | 8.381870 | 191.749181 | False |
| 272 | 40.827538 | 67.740788 | -32.455127 | 167.936702 | True |
| 288 | 38.031105 | 64.944355 | -35.251559 | 165.140269 | True |
| 304 | 73.586906 | 100.500156 | 8.946890 | 192.053423 | False |
| 320 | -15.297844 | -15.049068 | -126.551044 | 96.452908 | True |
| 336 | -62.787933 | -74.154564 | -197.271946 | 48.962818 | True |
| 352 | -38.528656 | -49.895287 | -173.012669 | 73.222095 | True |
| 368 | -114.818405 | -126.185036 | -214.634032 | -37.736039 | False |
| 384 | -110.436043 | -121.802673 | -211.566379 | -32.038968 | False |

| 仮の介入時点 | 観測合計 | 補正後の見かけの効果 / 時点 | 補正後の見かけの効果 / 観測合計 (%) | 誤差半径 / 観測合計 (%) |
| --- | --- | --- | --- | --- |
| 256 | 6749.055 | 1.564 | 1.483 | 1.358 |
| 272 | 6778.662 | 1.058 | 0.999 | 1.478 |
| 288 | 6740.789 | 1.015 | 0.963 | 1.486 |
| 304 | 6765.535 | 1.570 | 1.485 | 1.353 |
| 320 | 6694.704 | -0.235 | -0.225 | 1.666 |
| 336 | 6689.479 | -1.159 | -1.109 | 1.840 |
| 352 | 6630.267 | -0.780 | -0.753 | 1.857 |
| 368 | 6599.934 | -1.972 | -1.912 | 1.340 |
| 384 | 6515.132 | -1.903 | -1.870 | 1.378 |

:::

#### 累積誤差300は、大きいのか？

これも実行結果ではなく、単位を読むための例です。1日平均100、期間64日なら、介入がない場合の合計は6,400。累積誤差300は1日平均約4.69、期間合計の約4.69%になります。

| 知りたい効果 | 64日間の効果量 | 累積誤差300との関係 |
|---|---:|---|
| +2% | 128 | 誤差が効果の約2.34倍 |
| +5% | 320 | 誤差が効果の約94% |
| +10% | 640 | 誤差が効果の約47% |

同じ300でも、知りたい効果によって意味が変わります。

点の位置と区間の幅も分けて読んでください。補正後の点が+10、区間が−290〜+310なら、点は0に近くても、誤差半径は300、全幅は600です。「0を含んだ」だけでは、小さな効果を読めるとは言えません。

### 3. 知りたい大きさの効果を加えてみる

A/Aで0を含みやすい区間が作れたとしても、広すぎれば本当の効果まで見逃します。そこで、未介入の結果変数だけに+2%、+5%、+10%の持続的な効果を加えます。

ループの中で行う計算は次の部分です。対照時系列と過去の入力は変わらないので、元の予測をそのまま使えます。

```python
injected = untreated * (1 + lift)
true_effect_path = injected - untreated
estimated_effect_path = injected - forecast
```

同じA/A補正を適用した後、`injection_summary` で、加えた効果と推定した効果、平均絶対相対誤差、検出率を確認します。

相対誤差の分母は、**加えた効果そのもの**です。真の効果が300なのに240と推定したら、絶対相対誤差は20%。普段の結果の水準に対する誤差率とは意味が違います。

大きな効果は検出できても、+2%だけ難しいなら、知りたい効果量に対して誤差や区間が大きい可能性があります。逆に検出率が高くても、推定した大きさが大きく外れるなら、効果量の推定には課題が残ります。

ここは**効果の有無と、大きさの復元を分けて読む**ところです。平均推定値が真値に近くても、ウィンドウごとの過大・過小推定が相殺されていないか、相対誤差も見てください。

本文の検出率は「方向を問わず区間が0を含まない割合」です。正の効果を加えているので、推定の方向も併せて確認します。また、本文の検出率と相対誤差は0〜1の比率で、0.2なら20%です。

この結果は、重なるウィンドウに一定割合の効果を加えた場合の感度点検です。独立した実験の検出力や、別の効果の形に対する性能まで示すものではありません。

#### 効果注入の出力

| 注入効果 | 真の累積効果（平均） | 推定累積効果（平均） | 平均絶対相対誤差（0〜1） | 経験的検出率（0〜1） |
| --- | --- | --- | --- | --- |
| +10% | 668.484131 | 662.502388 | 0.119980 | 1.000000 |
| +2% | 133.696671 | 127.714919 | 0.599901 | 0.555556 |
| +5% | 334.241669 | 328.259914 | 0.239960 | 1.000000 |

検出率と相対誤差は0〜1の比率です。

### 4. 最後に残した64時点で、もう一度A/A

ここまでの予測検証、区間補正、効果注入に使わなかった最後の64時点で、固定した設定を確認します。

補正には全開発ウィンドウから求めた `dev_bias` と `dev_radius` だけを使います。

```python
# IMPORTANT: calibration uses development A/A only.
final_corrected = final_cumulative - dev_bias
final_lower = final_corrected - dev_radius
final_upper = final_corrected + dev_radius
```

出力では、最後の区間の累積誤差、補正後の点と区間、観測合計に対する割合を確認できます。

0を含み、誤差も開発区間と似ていれば、この未使用区間では整合的な結果です。ただし、最後の1回で0を含んでも、開発A/Aで繰り返された問題が消えるわけではありません。区間が関心のある効果より広すぎないかも見ます。

この結果を見てモデルや入力を選び直したら、この区間は新しい設定の未使用検証には使えません。最後に取っておく意味を保つには、**結果を見る前に設定を固定する**必要があります。

#### 最終A/Aの出力

![最後の未使用区間の予測と見かけの効果](/images/timesfm3-causalimpact/heldout-placebo.png)
*最後の未使用区間の予測と見かけの効果。*

| 項目 | 値 |
| --- | --- |
| MAE | 0.929034 |
| ME（観測−予測） | 0.50841 |
| 補正前の見かけの累積効果 | 32.538216 |
| バイアス補正後の累積効果 | 47.83606 |
| 累積効果の区間下限 | -48.178612 |
| 累積効果の区間上限 | 143.850731 |
| 0を含む | True |

:::details 未使用区間の観測合計に対する誤差の規模

| 項目 | 値 |
| --- | --- |
| 未使用区間の観測合計 | 6430.693848 |
| 補正後の見かけの効果 / 時点 | 0.747438 |
| 補正後の見かけの効果 / 観測合計 (%) | 0.743871 |
| 共通の誤差半径 / 観測合計 (%) | 1.493069 |

:::

## Part B：TimesFMを使う実益はあるか

ここまでは、固定した予測器をどう検証するかを見てきました。次は、その予測器としてTimesFMを使う利点を確かめます。

TimesFM 3では、事前学習済みモデルに対照時系列を直接渡せます。今回のように開始時点を変えて何度も検証するとき、時点ごとにモデルを追加学習せず試せるのが便利です。

ただ、対照時系列を入力できることと、実際に役立つことは別です。同じ開発ウィンドウで、対照時系列なしの予測も作ります。

```python
# Assess the value of controls separately from Part A's fixed-model validation.
univariate_mae = []
for i, cut in enumerate(DEV_CUTS):
    forecast_no_cov, _ = run_counterfactual(
        forecaster,
        y_context=y0[cut - CONTEXT:cut],
        horizon=DEV_H,
        covariates=None,
    )
    univariate_mae.append(np.mean(np.abs(actual_paths[i] - forecast_no_cov)))
```

`control_comparison` の表と線グラフで、対照あり・なしのMAEをウィンドウごとに比べます。

対照ありのMAEが複数のウィンドウで一貫して小さければ、そのデータと設定では追加情報が予測に寄与する根拠になります。改善が小さい、あるいは時期によって逆転するなら、使う実益は限定的かもしれません。誤差が増える場合は、変数の関連性や時点の対応も点検します。

もう1つ見るのが、Part Aの予測後半のMAEです。64時点を一度に出せても、後半で精度が崩れるなら、長い効果評価にはそのまま使いにくくなります。

ここで入力構成を変更するなら、変更後の構成で検証・補正をやり直します。すでに見た最終A/Aを、新しい構成の独立した最終確認として使い回すことはできません。

#### 対照時系列の比較結果

![対照時系列あり・なしのTimesFM予測のMAE比較](/images/timesfm3-causalimpact/control-comparison.png)
*対照時系列あり・なしのTimesFM予測のMAE比較。*

| 項目 | 値 |
| --- | --- |
| 対照ありの平均MAE | 1.264436 |
| 単変量の平均MAE | 1.464310 |
| 対照によるMAE改善率（0〜1） | 0.136497 |

:::details ウィンドウ別MAEの数値表

| 予測開始時点 | TimesFM＋対照 MAE | TimesFM単変量 MAE |
| --- | --- | --- |
| 256 | 1.305057 | 1.753503 |
| 272 | 1.066406 | 1.305800 |
| 288 | 0.994719 | 1.156478 |
| 304 | 1.250150 | 1.738248 |
| 320 | 0.843441 | 1.065987 |
| 336 | 1.094656 | 1.144870 |
| 352 | 1.062027 | 1.152703 |
| 368 | 1.802250 | 1.837837 |
| 384 | 1.961218 | 2.023365 |

:::

## Part C：固定した設定で効果を計算する

### 時点別の差と、補正後の総効果を分ける

合成データの介入後64時点を、同じ設定で分析します。ここでも入力するYは介入前までです。

```python
real_start = PRE - CONTEXT
real_covariates = controls[:, real_start:PRE + H]

real_cf, real_q = run_counterfactual(
    forecaster,
    y_context=y_treated[real_start:PRE],
    horizon=H,
    covariates=real_covariates,
)

observed_post = y_treated[PRE:]
estimated_effect = observed_post - real_cf
real_cumulative = float(estimated_effect.sum())

real_corrected = real_cumulative - dev_bias
real_lower = real_corrected - dev_radius
real_upper = real_corrected + dev_radius
```

ノートブックには、次の2種類の図が出ます。

- 観測値と反実仮想予測、時点別の差、累積した差の経路
- 期間全体の効果について、補正前の最終値と補正後の点・経験的区間を並べた図

経路図は補正前の値です。今回は64時点の累積誤差だけを補正しているため、その補正を日別に割り振ったり、途中時点の区間へ広げたりはしません。

補正前後で最終値が大きく動くなら、過去の累積バイアスが結論に強く影響しています。その偏りが今回の区間にも当てはまりそうかを考えます。

経験的区間が0を含むなら、現在の誤差範囲では0と区別できません。含まない場合も、因果仮定や開発A/Aの問題まで解決したわけではありません。前の検証で残った限界を、そのまま持ち込んで読みます。

#### 介入効果の計算結果

![介入後の観測値・反実仮想予測と補正前の時点別・累積効果](/images/timesfm3-causalimpact/intervention-paths.png)
*介入後の観測値・反実仮想予測と補正前の時点別・累積効果。*

![期間全体の効果：補正前の値と補正後の値・経験的区間](/images/timesfm3-causalimpact/intervention-total.png)
*期間全体の効果：補正前の値と補正後の値・経験的区間。*

| 項目 | 値 |
| --- | --- |
| 補正前の累積効果 | 560.765747 |
| バイアス補正後の累積効果 | 576.063591 |
| 経験的区間の下限 | 480.04892 |
| 経験的区間の上限 | 672.078262 |
| 0を含む | False |
| 補正後の累積効果の絶対値 / 誤差半径 | 5.999745 |

### p10–p90は、累積効果の区間ではない

予測図には、TimesFMが返す時点別のp10–p90も表示します。これは各時点の予測分位点の範囲です。名目上は中央80%に対応しますが、実際の被覆率は別途検証が必要です。

これを単純に足して、64時点の累積効果の区間にすることはできません。合計の不確実性には、時点間で誤差がどう一緒に動くかも関わるからです。

今回の経験的区間は、過去の**64時点の誤差を先に合計した値**から作っています。時点別の予測分位点とは、材料も目的も違います。

### 真値との照合は、最後の答え合わせ

合成データでは真の $Y(0)$ と加えた効果が分かるので、`positive_summary` と `null_summary` で累積効果やMAEを照合できます。

- 真の累積効果が区間内に入ったか
- 加えた10%の効果を、どのくらい近く復元できたか
- 効果なしのケースで、見かけの効果がどのくらい残ったか

これは今回の合成例での答え合わせです。2つのケースは同じ介入前の結果と対照時系列を使っており、独立した2回の検証ではありません。

実データでは真値が分からないので、知りたい累積効果の大きさを先に決め、推定値と区間の半径をそこに照らします。効果を%で表す場合も、分母となる「介入がなかった場合の合計」は推定値です。介入後の観測合計には効果が含まれるため、A/Aと同じ感覚で基準にしないようにします。

#### 合成データの真値との照合

| シナリオ | 真の累積効果 | 補正後の累積効果 | 反実仮想MAE | 補正前の時点別効果MAE | 真の累積効果を含む |
| --- | --- | --- | --- | --- | --- |
| 効果あり | 634.752808 | 576.063591 | 1.420385 | 1.420384 | True |
| 効果なし | 0.000000 | -58.689217 | 1.420385 | 1.420385 | True |

## BSTSと比べるときは、予測器と区間を分ける

ノートブックの付録では、RのCausalImpactが使うベイズ構造時系列モデル（BSTS）を同じデータに適用します。

本文を実行したPythonランタイムからRを別プロセスで動かすので、ランタイムの切り替えは不要です。初回のパッケージ準備には時間がかかることがあり、BSTSの計算はCPUで行います。

### 比較条件をそろえる

入力長256、予測長64、2本の対照時系列、予測開始時点をTimesFMとそろえます。BSTSは各開始時点で学習し直し、予測区間の結果は学習に使いません。

付録ではMCMC反復数を4,000、`prior.level.sd` を0.01に固定しています。基礎水準と固定の回帰係数を使い、追加の季節成分は入れません。今回は対照変数が周期的な動きを持っているためです。

`prior.level.sd=0.01` は[CausalImpactのデフォルト値](https://google.github.io/CausalImpact/CausalImpact.html)ですが、このデータに最適な設定という意味ではありません。シードや反復数に対する安定性、MCMCの収束も別途確認が必要です。

まずは `bsts_comparison` でMAEと補正前の累積誤差を比べます。これで、どちらの予測器がどのウィンドウで外れたのかを見られます。その後で区間の比較へ進みます。

#### BSTSとの予測比較

![同じ条件でのTimesFMとBSTSのMAE・累積誤差比較](/images/timesfm3-causalimpact/bsts-forecast-comparison.png)
*同じ条件でのTimesFMとBSTSのMAE・累積誤差比較。*

:::details 両モデル・全ウィンドウの予測誤差表

| モデル | 予測開始時点 | MAE | MAE / 観測平均 (%) | 補正前の累積誤差 / 観測合計 (%) |
| --- | --- | --- | --- | --- |
| TimesFM | 256 | 1.305 | 1.238 | 1.084 |
| TimesFM | 272 | 1.066 | 1.007 | 0.602 |
| TimesFM | 288 | 0.995 | 0.944 | 0.564 |
| TimesFM | 304 | 1.250 | 1.183 | 1.088 |
| TimesFM | 320 | 0.843 | 0.806 | -0.229 |
| TimesFM | 336 | 1.095 | 1.047 | -0.939 |
| TimesFM | 352 | 1.062 | 1.025 | -0.581 |
| TimesFM | 368 | 1.802 | 1.748 | -1.740 |
| TimesFM | 384 | 1.961 | 1.927 | -1.695 |
| BSTS | 256 | 1.594 | 1.511 | 1.450 |
| BSTS | 272 | 1.137 | 1.074 | 0.917 |
| BSTS | 288 | 0.880 | 0.835 | -0.017 |
| BSTS | 304 | 0.840 | 0.795 | 0.322 |
| BSTS | 320 | 1.287 | 1.230 | -1.077 |
| BSTS | 336 | 1.474 | 1.410 | -1.381 |
| BSTS | 352 | 1.218 | 1.175 | -1.070 |
| BSTS | 368 | 2.031 | 1.970 | -1.969 |
| BSTS | 384 | 1.997 | 1.962 | -1.835 |

:::

### 3つの結果は、何が違う？

| 表示される方法 | 点推定と区間の作り方 | 比較で分かること |
|---|---|---|
| `TimesFM + empirical` | TimesFM予測に、過去の累積誤差による補正を適用 | 本文の方法 |
| `BSTS + empirical` | BSTS予測に、同じ手順で求めたBSTS側の補正を適用 | 経験的な区間の作り方をそろえ、予測器を変えた差 |
| `BSTS native` | BSTS自体の事後分布から点と区間を計算 | 予測器と不確実性の計算を組み合わせた結果 |

「同じ経験的補正」とは、同じ数値を使うという意味ではありません。各モデルの過去の累積誤差から、それぞれバイアスと半径を求めます。

BSTS自体の区間には、経験的バイアス補正を追加しません。[公式実装](https://github.com/google/CausalImpact/blob/master/R/impact_inference.R)では、予測サンプルの経路を累積してから区間を求めています。時点別の区間を単純に足したものではありません。

付録ではBSTSの事後区間を90%に設定し、経験的補正にも0.90を使います。ただし、同じ数字でも同じ被覆保証を意味しない点は、ここでも変わりません。

#### 3方式のA/A比較

![TimesFM経験的補正・BSTS経験的補正・BSTS事後区間のA/A比較](/images/timesfm3-causalimpact/bsts-placebo-comparison.png)
*TimesFM経験的補正・BSTS経験的補正・BSTS事後区間のA/A比較。*

| 方法 | ウィンドウ数 | 0を含まない (%) | 見かけの効果の絶対値の比率・中央値 (%) | 区間全幅の比率・中央値 (%) |
| --- | --- | --- | --- | --- |
| TimesFM + empirical | 9 | 44.444 | 1.109 | 2.956 |
| BSTS + empirical | 9 | 22.222 | 1.282 | 3.303 |
| BSTS native | 9 | 77.778 | 1.077 | 1.548 |

比率の分母は各ウィンドウの観測合計です。右の2列は、ウィンドウごとに求めた比率の中央値を示します。

### 小さな効果の検出率だけで、勝ち負けを決めない

A/Aの `app_aa` では、0を含まない割合と区間の全幅を比べます。続く `app_injection` では+2%、+5%、+10%の効果量の誤差と検出率を見ます。

付録は正の効果を加えているため、**区間の下限が0より大きい場合**を正の効果の検出として数えます。方向を問わない本文の指標とは定義が違います。付録内の3方法は同じ定義で、比率も0〜100の%表示にそろえています。

たとえば、小さな効果をよく検出する方法が、A/Aでも頻繁に0を外していたらどうでしょうか。効果を拾いやすい代わりに、介入なしでも効果を報告しやすい可能性があります。逆に、A/Aで0を含みやすくても区間が広ければ、+2%の効果を見逃すかもしれません。

MAE、A/A、効果注入で優劣が分かれることもあります。その場合は「どちらが常に優秀か」より、**自分たちが知りたい効果量を、許容できる誤差と区間の幅で読めるか**を基準にします。

最後の `app_final` では、最終A/Aと介入例も同じ設定で並べます。ここまで見てモデルを選んだ場合、その選択は探索的です。選び直したモデルの確認には、別の未使用区間が必要になります。

#### 3方式の効果注入

| 方法 | 注入効果 (%) | 平均絶対相対誤差 (%) | 正の効果の検出 (%) |
| --- | --- | --- | --- |
| TimesFM + empirical | 2.0 | 59.990 | 55.556 |
| BSTS + empirical | 2.0 | 63.947 | 44.444 |
| BSTS native | 2.0 | 55.771 | 66.667 |
| TimesFM + empirical | 5.0 | 23.996 | 100.000 |
| BSTS + empirical | 5.0 | 25.579 | 100.000 |
| BSTS native | 5.0 | 22.308 | 100.000 |
| TimesFM + empirical | 10.0 | 11.998 | 100.000 |
| BSTS + empirical | 10.0 | 12.789 | 100.000 |
| BSTS native | 10.0 | 11.154 | 100.000 |

#### 3方式の最終A/A・介入効果

| 区間 | 方法 | 累積効果の推定値 | 下限 | 上限 | 真の累積効果 | 0を含む | 真値を含む |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 最終A/A | TimesFM + empirical | 47.836 | -48.179 | 143.851 | 0.000 | True | True |
| 最終A/A | BSTS + empirical | 89.839 | -50.431 | 230.110 | 0.000 | True | True |
| 最終A/A | BSTS native | 18.884 | -45.783 | 87.099 | 0.000 | True | True |
| 介入効果の例 | TimesFM + empirical | 576.064 | 480.049 | 672.078 | 634.753 | False | True |
| 介入効果の例 | BSTS + empirical | 629.519 | 489.248 | 769.790 | 634.753 | False | True |
| 介入効果の例 | BSTS native | 558.564 | 497.158 | 621.935 | 634.753 | False | False |

## おわりに

キャンペーン後の売上と予測の差を読むなら、まず「キャンペーンがなかった過去で、同じくらいの差が出ないか」を確認したいところです。その差が小さくても、次は知りたい大きさの効果を加えて、検出と効果量の復元を確かめます。

TimesFM 3は、対照時系列を含む反実仮想予測を追加学習なしに試す入口になります。実務で使う判断には、そこで得られた予測精度だけでなく、累積誤差、区間の幅、因果仮定までが関わります。

ノートブックを試すなら、まず自分が知りたい変化を+2%なのか+10%なのか決めて、その行をA/Aと効果注入の両方で読んでみてください。「予測がよく当たる」から一歩進んで、**その精度で、知りたい効果を読めそうか**を考えるための検証です。

## 参考

- [Brodersen et al. (2015), Inferring causal impact using Bayesian structural time-series models](https://arxiv.org/abs/1506.00356)：CausalImpactの元論文
- [CausalImpact公式ドキュメント](https://google.github.io/CausalImpact/CausalImpact.html)：因果仮定、モデル設定、出力の定義
- [CausalImpactの累積効果・区間の実装](https://github.com/google/CausalImpact/blob/master/R/impact_inference.R)：BSTSの事後区間の計算
- [Google ResearchによるTimesFM-3の紹介](https://research.google/blog/timesfm-3-a-zero-shot-foundation-model-for-multivariate-forecasting/)：共変量と予測の機能
- [TimesFM 3.0の公式モデルカード](https://huggingface.co/google/timesfm-3.0-pytorch)：モデルとライセンス
